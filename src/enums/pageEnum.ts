export enum PageEnum {
  // login
  LOGIN = '/login',
  // redirect
  REDIRECT = '/redirect',
  // homepage
  HOME = 'dashboard',
  // history
  HISTORY = 'history',
  // analyze
  ANALYZE = 'analyze',
  // user
  USER = 'user',
  // settings
  SETTINGS = 'settings',
  // error
  ERROR_NAME = 'ErrorPage',
}
